# DurationModulatedDynamics
Contains the code to reproduce figures from the manuscript "Duration-modulated neural population dynamics in humans during BMI controls"

```
# Create environment
conda create -n dsa
conda activate dsa

# Install requirements
pip install -r requirements.txt
pip install -e .

# Run figure generation script
python -m scripts.figure_generation_MASTER
```
